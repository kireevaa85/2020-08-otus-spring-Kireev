spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
#    password: sa
    driver-class-name: org.h2.Driver
    initialization-mode: always
    schema: schema.sql
    data: test-data.sql
  shell:
    interactive:
      enabled: false
