spring:
  data:
    mongodb:
      port: 0
      database: test
  shell:
    interactive:
      enabled: false

mongock:
  change-logs-scan-package:
    - ru.otus.mongock.testchangelog

logging:
  level:
    root: error
